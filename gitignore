# Data — never commit
data/raw/
data/processed/

# Local dev
.env
venv/
__pycache__/
*.pyc
.DS_Store

# Model artifacts — COMMIT THESE (Railway loads them at startup)
# backend/models/*.pkl   <-- intentionally not ignored
# backend/models/*.json  <-- intentionally not ignored

# Node
node_modules/
frontend/dist/
